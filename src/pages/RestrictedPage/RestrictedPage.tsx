// Core
import * as React from 'react';
// Components
import Layout from '@components/Layout';

// Helpers

const RestrictedPage: React.FC = () => {
    return <Layout title="Доступ запрещен">Доступ запрещен</Layout>;
};

// Exports
export default RestrictedPage;
